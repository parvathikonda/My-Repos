# Written by WATO
# encoding: utf-8


host_contactgroups = [
  ( 'Techwave', ['/' + FOLDER_PATH + '/+'], ALL_HOSTS ),
] + host_contactgroups


host_groups = [
  ( 'ISB', ['/' + FOLDER_PATH + '/+'], ALL_HOSTS ),
] + host_groups

